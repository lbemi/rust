[package]
name = "ex"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

# panic 终止调用栈,减少二进制文件大小
[profile.release]
panic = 'abort'
